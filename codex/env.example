# Default environment variables for multi_agent_example.py
# Copy this to .env and fill in your values.
#
# The script loads .env files in this order:
#   1. codex/.env (script directory) - for default settings
#   2. .env (current working directory) - for project-specific overrides

# OpenAI API Key (required)
OPENAI_API_KEY=sk-...

# Default model for agents (optional, agents can override in their config)
# DEFAULT_MODEL=gpt-5

# Codex MCP timeout in seconds (default: 360000 = 100 hours)
# CODEX_MCP_TIMEOUT=360000
