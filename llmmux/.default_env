# llmmux Default Environment Configuration
#
# This file contains default configuration values for llmmux.
# User-specific overrides should go in ~/.llmmux/.env
#
# NOTE: This file is synced from the project repository to ~/.llmmux/
# To modify defaults, edit the source file and re-run sync.

# ==============================================================================
# Provider Command Templates
# ==============================================================================
#
# These templates define how to invoke each LLM provider.
# Use {prompt} as a placeholder for the actual prompt text.
#
# The prompt placeholder will be automatically replaced with the appropriate
# instruction when spawning agents.

# Gemini CLI (headless mode with -p flag)
# Note: Gemini with -p runs in one-shot mode and may not persist sessions
PROVIDER_gemini=gemini -p '{prompt}'

# Cursor Agent CLI
PROVIDER_agent=agent '{prompt}'

# Codex CLI
PROVIDER_codex=codex '{prompt}'

# Claude CLI
PROVIDER_claude=claude '{prompt}'

# OpenCode CLI
PROVIDER_opencode=opencode '{prompt}'

# ==============================================================================
# Provider Skill/Agent Directories
# ==============================================================================
#
# Location of provider-specific agents and skills
# These are used by agents to discover available capabilities

# Cursor
CURSOR_AGENTS_DIR=~/.cursor/agents
CURSOR_SKILLS_DIR=~/.cursor/skills
CURSOR_COMMANDS_DIR=~/.cursor/commands

# Claude
CLAUDE_AGENTS_DIR=~/.claude/agents
CLAUDE_SKILLS_DIR=~/.claude/skills
CLAUDE_COMMANDS_DIR=~/.claude/commands

# Gemini
GEMINI_AGENTS_DIR=~/.gemini/agents
GEMINI_SKILLS_DIR=~/.gemini/skills
GEMINI_COMMANDS_DIR=~/.gemini/commands

# Codex
CODEX_AGENTS_DIR=~/.codex/agents
CODEX_SKILLS_DIR=~/.codex/skills
CODEX_COMMANDS_DIR=~/.codex/commands

# ==============================================================================
# llmmux Directories
# ==============================================================================

# Main llmmux directory (typically ~/.llmmux)
LLMMUX_HOME=~/.llmmux

# State directory for session data
LLMMUX_STATE_DIR=~/.llmmux/state

# Artifacts directory for generated files
LLMMUX_ARTIFACTS_DIR=~/.llmmux/artifacts

# ==============================================================================
# Session Defaults
# ==============================================================================

# Default session name (can be overridden with --session-name)
LLMMUX_DEFAULT_SESSION=llmmux

# Default provider (can be overridden with --provider)
# Uncomment and set to: gemini, agent, codex, claude, or opencode
# LLMMUX_DEFAULT_PROVIDER=agent

# ==============================================================================
# tmux Configuration
# ==============================================================================

# Default tmux window name for orchestrator
LLMMUX_ORCHESTRATOR_WINDOW=orchestrator

# Whether to auto-attach after creating session (true/false)
LLMMUX_AUTO_ATTACH=false

# ==============================================================================
# Logging and Debugging
# ==============================================================================

# Enable verbose output (true/false)
LLMMUX_VERBOSE=false

# Log level (DEBUG, INFO, WARNING, ERROR)
LLMMUX_LOG_LEVEL=INFO

# ==============================================================================
# Notes for Users
# ==============================================================================
#
# To override any of these settings:
# 1. Create ~/.llmmux/.env (it won't be overwritten by sync)
# 2. Add your overrides in the same format (KEY=value)
# 3. Your .env values will take precedence over these defaults
#
# Example ~/.llmmux/.env:
#   LLMMUX_DEFAULT_PROVIDER=agent
#   LLMMUX_DEFAULT_SESSION=myproject
#   LLMMUX_AUTO_ATTACH=true
